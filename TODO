- __ Escaping__:
    - right now, the system forbids `\`, `(` or `)` in filenames, which is hard to
      work with. Although I hate it with passion, maybe escaping might be an option.
      Ugh :\
- Empty Expression, with type `forall a. a`
    - issue: Every file has at least one line ><